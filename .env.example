# ==============================================================================
# NexGen Studio — Frontend Environment Variables Template
# ==============================================================================
# In development, point to your local backend API (default: http://localhost:5000)
# In production, set to your backend API URL (e.g. https://api.YOUR_PRODUCTION_DOMAIN.example)
# or set to /api if the frontend and backend are hosted on the same origin.
VITE_API_URL=http://localhost:5000

