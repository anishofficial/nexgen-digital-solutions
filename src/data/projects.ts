import type { Project } from '../types';

// Content in the work section has been cleared as requested.
// To add new client case studies later, append Project objects to this array.
export const projectsData: Project[] = [];
